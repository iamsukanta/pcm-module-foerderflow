# Browser-facing backend API base (exposed to the client bundle).
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Server-side backend base (used by Server Components inside docker network).
BACKEND_INTERNAL_URL=http://backend:8000/api

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
