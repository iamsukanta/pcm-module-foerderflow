"""csv_import — universal CSV import system (port of foerderflow lib/import/*).

Detector/tokenizer, profile-based parser, duplicate hashing, Kostenbereich
heuristics, builtin profiles, field-hint mapping suggestion, and the persistence
pipeline (bank-account resolution, type classification, booking-rule auto-apply,
saldo consistency).
"""
